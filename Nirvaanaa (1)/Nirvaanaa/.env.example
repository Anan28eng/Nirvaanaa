# Example environment variables for Nirvaanaa
# Copy to .env.local and fill in real values before deploying.

# Primary DB connection (preferred)
DB_URI=mongodb+srv://username:password@cluster.example.mongodb.net/nirvaanaa

# Fallback (legacy) - MONGODB_URI is still supported
MONGODB_URI=

# NextAuth settings
NEXTAUTH_URL=https://nirvaanaa.in
NEXTAUTH_SECRET=replace_with_a_long_random_string_here

# Public API base (used by client-side code)
NEXT_PUBLIC_API_URL=https://nirvaanaa.in

# Cloudinary (if used)
CLOUDINARY_URL=cloudinary://api_key:api_secret@cloud_name

# Socket control - set true to disable socket emissions in server
DISABLE_SOCKET=true

# Optional: enable or disable debug logging
NODE_ENV=production
