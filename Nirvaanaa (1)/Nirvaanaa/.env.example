# Example environment variables for Nirvaanaa
# Copy to .env.local and fill in real values before deploying.

# Primary DB connection (preferred for Atlas)
MONGO_URI=mongodb+srv://username:password@cluster.example.mongodb.net/nirvaanaa?retryWrites=true&w=majority
# Legacy / alternative names
DB_URI=
MONGODB_URI=

# NextAuth settings
NEXTAUTH_URL=https://nirvaanaa.in
NEXTAUTH_SECRET=replace_with_a_long_random_string_here

# Public API base (used by client-side code)
NEXT_PUBLIC_API_URL=https://nirvaanaa.in

# Cloudinary (if used)
CLOUDINARY_URL=cloudinary://api_key:api_secret@cloud_name

# Socket control - set true to disable socket emissions in server (server-side)
DISABLE_SOCKET=true
# Client-visible socket disable (prefix with NEXT_PUBLIC_ so it is inlined in the client)
NEXT_PUBLIC_DISABLE_SOCKET=true

# Optional: enable or disable debug logging
NODE_ENV=production
